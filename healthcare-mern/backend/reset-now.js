const mongoose = require(\"mongoose\"); const User = require(\"./models/User\"); require(\"dotenv\").config(); async function fix() { await mongoose.connect(process.env.MONGODB_URI); const user = await User.findOne({ email: \"madudamian25@gmail.com\" }); if (user) { user.password = \"password123\"; await user.save(); console.log(\" Password reset to: password123\"); } await mongoose.disconnect(); } fix();
